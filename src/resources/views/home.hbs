<div class="mt-4">
  <div class="row">
    {{#each products}}
      <div class="col-3 mb-4">
        <div class="card product-item">
          <a href="/products/{{this.slug}}">
            <img
              class="card-img-top"
              src="{{this.img}}"
              alt="{{this.name}}"
            /></a>
          <div class="card-body">
            <a href="/products/{{this.slug}}">
              <h5 class="card-title product-name">{{this.name}}</h5></a>
            <p class="card-text">{{this.description}}</p>
            <a href="#" class="btn btn-primary float-right">MUA</a>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>